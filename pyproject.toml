[build-system]
requires = [ "poetry>=0.12",]
build-backend = "poetry.masonry.api"

[tool.poetry]
name = "project"
version = "0.0.0"
description = "Mental Health comorbitidy dataset core project."
authors = ["Amey Hengle <ameyhengle22@gmail.com>"]

[tool.poetry.dependencies]
python = ">=3.8,<3.10"
pandas = "^1.5.1"
loguru = "^0.6.0"
sklearn = "^0.0"
clean-text = "^0.6.0"
torch = "^1.13.0"
simpletransformers = "^0.63.9"
sentence-transformers = "^2.2.2"
rank-bm25 = "^0.2.2"
openai = "^0.25.0"
